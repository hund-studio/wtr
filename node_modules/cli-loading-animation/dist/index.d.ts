export interface LoaderActions {
    start(): void;
    stop(): void;
}
export interface LoaderOptions {
    spinner?: Spinner;
    clearOnEnd?: boolean;
}
export interface Spinner {
    frames: string[];
    interval: number;
}
export declare const loading: (message: string, options?: LoaderOptions | undefined) => LoaderActions;
